# 实施状态

## 已完成

- 创建 `wechat-codex/` 工程结构。
- 实现公众号明文模式 gateway。
- 实现本地 JSON 任务队列。
- 实现微信文本指令解析。
- 实现 runner 拉任务和回传结果。
- 支持 runner dry-run 验证模式。
- 补充 `.env.example` 和 README。
- 完成本地端到端 dry-run 验证。
- 验证公众号 URL GET 签名校验。
- 验证只读任务：微信 XML -> gateway 入队 -> runner dry-run -> 状态查询。
- 验证写入任务：创建后等待批准；未批准不会执行；批准后 runner 执行并回传结果。
- 接入 Cloudflare Tunnel quick tunnel。
- 公众号服务器配置可提交，真实微信消息可入队。
- 真实微信只读任务已由 Codex 执行成功。
- 已为公众号被动回复增加长文本截断保护。
- 新增 `完整 <任务ID>` 指令，用于返回本机完整结果文件路径。
- runner 会把新任务完整回答保存到 `data/results/<任务ID>.md`。
- 新增 `全文 <任务ID>` 和 `全文 <任务ID> <段号>`，可在微信里分段阅读完整回答。
- 新增 `文件 <任务ID>`，可返回带 token 的手机可打开 Markdown 链接。
- 新增任务命名：`任务 <任务名> 项目 artical 只读/修改 <指令>`。
- 新增 `任务列表`，可查看最近任务及状态。
- 所有任务操作支持使用 `<任务名或ID>`。
- 新增 `命名 <任务名或ID> <新任务名>`，可给旧任务补名字。
- 新增任务分类：`归类 <任务名或ID> <分类名>`。
- 新增 `分类列表` 和 `分类 <分类名>`，可像文件夹一样查看同类任务。
- 创建任务时可直接指定分类：`任务 <任务名> 分类 <分类名> 项目 artical 只读/修改 <指令>`。

## 待完成

- 按需部署云服务器 gateway。
- 按需实现公众号主动结果推送。
- 按需实现后台页面或公网日志查看。
